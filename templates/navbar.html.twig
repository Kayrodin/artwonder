<nav class="navbar navbar-expand shadow-sm">
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <img src="{{ asset('uploads/logo/logo.png') }}" class="catlogo" alt="">
        <ul class="navbar-nav ml-auto" id="menu">
            <li class="nav-item">
                <a class="nav-link" id="inicio" href="{{ path('home') }} ">Inicio</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('help') }}">Ayuda</a>
            </li>
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ usuario.nombre }}
                    </a>

                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('usuario_edit') }}"><i class="fa fa-cog"></i> Ajustes</a>
                        <a class="dropdown-item" href="{{ path('marca_autor_index') }}"><i class="fa fa-signature"></i> Marcas</a>
                        <a class="dropdown-item" href="{{ path('wond_art_index') }}"><i class="fa fa-images"></i> WondArts</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fa fa-sign-out-alt"></i> Salir</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ usuario.nombre }}
                    </a>

                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('usuario_edit') }}"><i class="fa fa-cog"></i> Ajustes</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fa fa-sign-out-alt"></i> Salir</a>
                    </div>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>