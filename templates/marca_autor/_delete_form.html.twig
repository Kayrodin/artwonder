<form method="post" action="{{ path('marca_autor_delete', {'id': marca_autor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marca_autor.id) }}">
    <button class="btn">Delete</button>
</form>
